import React from 'react';
export function EvidenceGraphView({onSelectServiceFromGraph}:{onSelectServiceFromGraph:(s:string)=>void}) {
  const nodes=[['Deployment v3.18.2','deployment'],['Retry config changed','commit'],['DB connections ↑','metric'],['DB latency ↑','metric'],['Payment failures ↑','incident']];
  return <div className="max-w-6xl mx-auto space-y-5"><div><div className="text-xs uppercase tracking-widest text-sky-300">Evidence graph</div><h1 className="text-3xl text-white font-semibold mt-1">Causal chain</h1><p className="text-sm text-slate-400 mt-1">Solid links are observed; dashed links are inferred.</p></div><div className="rounded-xl bg-[#131b31] border border-blue-400/10 p-10 flex items-center justify-between">{nodes.map(([label,type],i)=><React.Fragment key={label}><button onClick={()=>label.includes('DB')&&onSelectServiceFromGraph('postgresql')} className="w-36 min-h-24 rounded-xl border border-blue-400/20 bg-[#0a1228] p-3 text-center"><div className="text-[10px] uppercase text-sky-300">{type}</div><div className="text-xs text-white mt-2">{label}</div></button>{i<nodes.length-1&&<div className="text-slate-500 text-2xl">→</div>}</React.Fragment>)}</div></div>
}