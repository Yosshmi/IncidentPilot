import React from 'react';
export function FilterScopeModal({isOpen,onClose,cluster,onClusterChange,timeWindow,onTimeWindowChange,selectedNamespace,onNamespaceChange}:{isOpen:boolean;onClose:()=>void;cluster:string;onClusterChange:(v:string)=>void;timeWindow:string;onTimeWindowChange:(v:string)=>void;selectedNamespace:string;onNamespaceChange:(v:string)=>void}) {
  if(!isOpen)return null; return <div className="fixed inset-0 z-[100] bg-black/60 grid place-items-center"><div className="w-[440px] rounded-xl bg-[#131b31] border border-blue-400/20 p-5"><h2 className="text-white font-medium">Dashboard scope</h2><div className="mt-4 space-y-3">{[['Cluster',cluster,onClusterChange,['us-east-cluster-04','us-west-cluster-02']],['Window',timeWindow,onTimeWindowChange,['15m','1h','6h']],['Namespace',selectedNamespace,onNamespaceChange,['prod-ingress','payments','platform']]].map(([label,value,setter,opts]:any)=><label key={label} className="block text-xs text-slate-400">{label}<select value={value} onChange={e=>setter(e.target.value)} className="mt-1 w-full bg-[#0a1228] rounded-lg p-2 text-slate-200">{opts.map((o:string)=><option key={o}>{o}</option>)}</select></label>)}</div><div className="mt-5 text-right"><button onClick={onClose} className="px-3 py-2 bg-sky-600 text-xs text-white rounded-lg">Apply</button></div></div></div>
}