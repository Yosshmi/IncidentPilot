import React from 'react';
import { Incident, IncidentStatus } from '../types';
export function IncidentsView({incidents,onSelectIncident,onOpenReportModal,onGoToInvestigation,onStatusChange}:{incidents:Incident[];onSelectIncident:(i:Incident)=>void;onOpenReportModal:()=>void;onGoToInvestigation:(i:Incident)=>void;onStatusChange:(id:string,s:IncidentStatus)=>void}) {
  return <div className="max-w-7xl mx-auto space-y-5"><div className="flex justify-between items-end"><div><div className="text-xs uppercase tracking-widest text-sky-300">Incident history</div><h1 className="text-3xl text-white font-semibold mt-1">Incidents</h1></div><button onClick={onOpenReportModal} className="px-3 py-2 bg-sky-600 rounded-lg text-xs text-white">New incident</button></div>
    <div className="rounded-xl bg-[#131b31] border border-blue-400/10 overflow-hidden"><div className="grid grid-cols-[100px_1.5fr_1fr_100px_120px] px-5 py-3 text-[10px] uppercase tracking-wider text-slate-500 border-b border-blue-400/10"><span>ID</span><span>Incident</span><span>Service</span><span>Status</span><span></span></div>
      {incidents.map(i=><div key={i.id} className="grid grid-cols-[100px_1.5fr_1fr_100px_120px] px-5 py-3 items-center border-b border-blue-400/5"><button onClick={()=>onSelectIncident(i)} className="text-left text-xs font-mono text-sky-300">{i.id}</button><div><div className="text-sm text-white">{i.title}</div><div className="text-xs text-slate-500">{i.severity} · {i.duration}</div></div><span className="text-xs text-slate-400">{i.affectedService}</span><select value={i.status} onChange={e=>onStatusChange(i.id,e.target.value as IncidentStatus)} className="bg-[#0a1228] text-xs text-slate-300 rounded p-1"><option>Active</option><option>Mitigated</option><option>Resolved</option></select><button onClick={()=>onGoToInvestigation(i)} className="text-xs text-sky-300 text-right">Investigate →</button></div>)}
    </div></div>
}